#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int isprime(int n){
    if (n<2) return 0;
    for(int i = 2; i<n; i++){
        if(n%i==0){
            return 0;
        }
    }
    return 1;
}

int main() {
    int s, e , flag = 0; 
    scanf("%d %d", &s,&e);
    for(int i = s; i<=e; i++){
        if(isprime(i)){
            printf("%d ",i);
            flag = 1;
        }
    
    }
    if(!flag){
        printf("No prime numbers found");
    }

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
