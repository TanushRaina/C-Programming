#include<stdio.h>
int f(int n){
    if(n== 0 ) return 0;
    if(n== 1) return 1;
    return f(n-1) + f(n-2);
    
}
void p(int x, int n){
    if(n==x) return; 
     int j =   f(x);
    printf("%d ",j);
    p(x+1,n);
    
    
}
int main(){
    int n;
    scanf("%d",&n);
    p(0,n);
    return 0;
}
