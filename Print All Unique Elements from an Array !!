#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int arr[100];
    for(int i = 0; i<n; i++){
        scanf("%d",&arr[i]);
    }
    int flag =0 ;
    for(int i =0; i<n; i++){
        int count = 0;
        for(int j = 0 ; j<n; j++){
            if(arr[i] == arr[j]){
                count++;
                
            }
        }
        if(count == 1) {
            printf("%d ",arr[i]);
            flag =1;
        }
    }
        if(flag == 0)
            printf("No unique elements");
        
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
