#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() {
    int sum =0 ,n,temp,r;
    scanf("%d",&n);
    temp = n;
    while(temp>0){
        r = temp%10;
        sum = sum*10 +r;
        temp = temp/10;

    }
    if (sum == n){
        printf("Palindrome");
    }
    else{
        printf("Not Palindrome");
    }
    

    
    return 0;
}
