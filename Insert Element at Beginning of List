#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int n;
int list[100];

void insertatbeginning(int element){
    for(int i=n-1;i>=0;i--){
        list[i+1]=list[i];
        
    }
    list[0]=element;
    n++;
}
int main() {
scanf("%d",&n);
    for(int i=0;i<n;i++){
scanf("%d",&list[i]);
    }
    int element;
    scanf("%d",&element);
    insertatbeginning(element);
for(int i=0;i<n;i++){
    printf("%d ",list[i]);
    
}
    return 0;
}
