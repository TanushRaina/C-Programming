#include <stdio.h>
void x(int *p){
    int n; 
    scanf("%d",&n);
    for(int i = 0; i<n; i++){
        scanf("%d",&p[i]);
    }
    for(int i = 0; i<n; i++){
        printf("%d ",p[i]);
    }
    return;
}
int main(){
    int arr[100];
    x(arr);
    return 0;
    
}
