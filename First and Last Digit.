#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int i, n  ;
    scanf("%d",&n);
    if (n<0){
    n = -n ;
        }
    int last = n%10;
    
    while (n>=10){
        n =n/10;
        
        
    }
    int first = n ;
    
    printf("%d %d", first , last );
        
    
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
